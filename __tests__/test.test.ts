import * as argType from '../src/common/argumentTypes'

describe('DoubleArgument', () => {
    describe('isType()', () => {
        it('same type', () => {
            expect(1).toBe(1)
        })
    })
})
